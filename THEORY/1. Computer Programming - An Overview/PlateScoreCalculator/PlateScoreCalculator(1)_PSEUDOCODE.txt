START
    int plate;
    
    WHILE (TRUE) {
        INPUT plate

        IF (TO_STRING(plate).LENGTH() == 5) {
            int remainder;
            int total = 0;
            int i = 0;

            while (i < TO_STRING(plate).LENGTH( )) {
                remainder = plate % 10
                total += remainder
                plate = plate / 10
                i++
            }

            int output = total % 10;
            PRINT output
            BREAK;
        }
        ELSE {
            PRINT "ERROR"
            CONTINUE;
        }
    }
END