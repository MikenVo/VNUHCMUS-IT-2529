START
    INPUT a, b, c

    IF (a == 0) {
        Linear Equation solver
    }
    ELSE {
        delta = b*b - 4*a*c

        IF (delta < 0) {
            PRINT "NO SOLUTION"
        }
        ELSE IF (delta == 0) {
            PRINT (-b)/(2*a)
        }
        ELSE {
            PRINT (-b - sqrt(delta))/(2*a)
            PRINT (-b + sqrt(delta))/(2*a)
        }
    }
END