Start the program
    Define int variables a, b and c.
    Define int variables min and max.

    Let the user prompt a, b and c.

    Set min and max equal to a.

    Check if min is greater than b.
        If true, set min equal to b.
            Check if min is greater than c.
                If true, set min equal to c.
                If false, ignore.
        If false, check if min is greater than c.
            If true, set min equal to c.
            If false, ignore.
    
    Check if max is smaller than b.
        If true, set max equal to b.
            Check if max is smaller than c.
                If true, set max equal to c.
                If false, ignore.
        If false, check if max is smaller than c.
            If true, sett max equal to c.
            If false, ignore.
    
    Print out min and max.
End the program.
        